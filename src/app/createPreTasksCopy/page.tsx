import { PageContent } from "@/components/PageContent";

export default function CreatePreTasksAltPage() {
  return (
    <PageContent
      title="Criar Pré-Tarefas (Alt)"
      description="Versão alternativa do formulário."
      highlights={[
        "Fluxo customizado para pré-tarefas.",
        "Validações específicas de negócio.",
        "Pré-visualização antes de salvar.",
      ]}
    />
  );
}
